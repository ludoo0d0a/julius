<?xml version="1.0" encoding="utf-8"?>
<!-- POI category for Android Auto; RECORD_AUDIO removed for Play policy simplicity.
     NAVIGATION_TEMPLATES removed: Play Console rejects non–navigation-category apps that declare it. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.RECORD_AUDIO"
        tools:node="remove" />

    <uses-permission
        android:name="androidx.car.app.NAVIGATION_TEMPLATES"
        tools:node="remove" />

    <application>
        <service
            android:name=".auto.VoiceAppService"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            tools:node="replace">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.POI" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="geo" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="google.navigation" />
            </intent-filter>
        </service>
    </application>
</manifest>
